/** @file Simulacio.java
    @brief Classe Simulacio
    @author Roman Barrera, Mireia Ferrer i Iria Auladell
*/

//@class Simulacio mostra per pantalla les dades de l evolucio.
public class Simulacio{

    /** @brief Mostra una sortida de text amb les dades de la regio
	@pre rv no buit.
	@post --- 
    @param territori Tipus de territori del que es vol obtenir dades (regio o territori total).
    @param tipusDada Dia actual o acumulada.
    */

    public void mostrarEstat(char territori, char tipusDada){
    }

    /** @brief Mostra un grafic de l'historial de dades de l'evolucio
	@pre rv no buit.
	@post --
    @param territori Tipus de territori del que es vol obtenir dades (regio o territori total).
    @param tipusDada Dia actual o acumulada.
    */
    public void mostrarGrafic(char territori, char tipusDada){    
    }

    /** @brief Permet a l'usuari aturar la simulacio.
    @pre --
    @post Simulacio aturada i resetejada
    */
    public static aturarSimulacio(){
            //metode presionar boto, si boto true executa
        bool botoAturar = false 
        }
}
